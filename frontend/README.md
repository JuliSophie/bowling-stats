# Bowling Stats Frontend

Next.js Frontend fuer Upload, OCR-Review und Statistik-Dashboard.

## Entwicklung

1. `npm install`
2. `.env.local.example` nach `.env.local` kopieren
3. `npm run dev`

Das Frontend erwartet standardmaessig die API unter `/api`.
